{% extends "base.html" %}
{% block content %}
<h3>Teacher Dashboard</h3>
<div class="d-grid gap-2 d-md-block">
  <a href="{{ url_for('register_student') }}" class="btn btn-primary me-2">
    👤 Register Student
  </a>
  <a href="{{ url_for('capture_attendance') }}" class="btn btn-success me-2">
    📹 Capture Attendance
  </a>
  <a href="{{ url_for('daily_attendance') }}" class="btn btn-info">
    📋 View Daily Attendance
  </a>
</div>
{% endblock %}
